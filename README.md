# check_integer
This program code will ask a user to provide an input. Additionally, the program will try to handle the error that may occur when a user type in the any type but int. 
The program would use "try" ... 'except' ... 'else' clause. 
The function should print the result if the operation is successful, if the operation is not successful your program should print (Unpeacefully converted to integer).
